This is a fork of the RSPerl module from [http://www.omegahat.org/RSPerl/]. It has been patched to install with 

```
PERL=`which perl` PERL5LIB=src R CMD INSTALL . --configure-args='--with-in-perl'
```
